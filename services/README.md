# Scientific services

This directory holds adapters and deployment definitions for runtimes that should remain isolated from the Python API, such as GROBID document parsing and an R/metafor deterministic analysis service. No service is added before its domain phase and license/fit review.

