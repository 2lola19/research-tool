# Infrastructure

The root `compose.yaml` is the supported local topology. Production deployment manifests are intentionally deferred. Container images use non-root runtime users and application-level health checks.

